#!/bin/sh
# Pre-commit hook for Canari project
# Runs linting checks before commit

cd frontend && bun run lint --quiet 2>/dev/null || {
  echo "❌ Linting failed. Use 'cd frontend && bun run lint:fix' to auto-fix issues."
  exit 1
}

exit 0
